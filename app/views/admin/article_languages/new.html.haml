.content-box-header
  %h3 New article language
.content-box-content
  = form_for @article_language, url: admin_article_languages_path(:id=>params[:id], :id_cate => params[:id_cate]), html: { method: :post } do |f|
    = render 'form', f: f
    %input.button{:type => 'submit', :value => 'Save'}
    = link_to 'Back', admin_article_path(:id=>params[:id], :id_cate => params[:id_cate]) ,:class => 'button'