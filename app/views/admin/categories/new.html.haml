.content-box-header
  %h3 New category
.content-box-content
  = form_for @category, url: admin_categories_path, html: { method: :post } do |f|
    = render 'form', f: f
    %input.button{:type => 'submit', :value => 'Save'}
    = link_to 'Back', admin_categories_path ,:class => 'button'