.content-box-header
  %h3 New language
.content-box-content
  = form_for @language, url: admin_languages_path, html: { method: :post } do |f|
    = render 'form', f: f
    %input.button{:type => 'submit', :value => 'Save'}
    = link_to 'Back', admin_languages_path ,:class => 'button'