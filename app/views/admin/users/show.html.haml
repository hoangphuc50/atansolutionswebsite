.content-box-header
  %h3 Detail user
.content-box-content
  - if !flash[:error].nil?
    #error_explanation.notification.information.png_bg
      %div.flash-notice
        %h2
          = flash[:error]
  %p
    %strong Name:
    = @users.name
  %p
    %strong Email:
    = @users.email
  = link_to 'Edit', edit_admin_user_path(@users),:class => 'button'
  //= link_to 'Reset password', "/admin/resetpassword?id=#{@users.id}",:confirm => 'Reset password. Are you sure?',:class => 'button'
  = link_to 'Back', admin_users_path,:class => 'button'
  -#- @article_language=ArticleLanguage.where(:category_id=>@category.id)
  -#%table
  -#  %thead
  -#    %tr
  -#      %th Name
  -#      %th Email
  -#      %th Created at
  -#      %th Updated at
  -#  %tbody
  -#    - @article_language.each do |user|
  -#      %tr
  -#        %td
  -#          = link_to(user.name, admin_user_path(user))
  -#        %td= user.email
  -#        %td= user.created_at
  -#        %td= user.updated_at